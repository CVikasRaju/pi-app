# pdf-export — Environment Variables
# Copy to .env and fill in values

CATALYST_PROJECT_ID=your-project-id
CLIENT_ORIGIN=http://localhost:3000
AUDIT_LOGGER_URL=http://localhost:9005

# NoSQL URL (used by historyStore — same table as chat-router)
# This function reads ConversationTurn table from the same Catalyst project

INTERNAL_AUDIT_SECRET=change-me-audit-secret
